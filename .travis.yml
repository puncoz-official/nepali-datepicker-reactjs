language: node_js
node_js:
  - "v12.8.1"
cache:
  directories:
    - node_modules
script:
  - npm test
deploy:
  provider: pages
  skip_cleanup: true
  github_token: $github_token
  local_dir: example/dist
  on:
    branch: master
